{
  "_README": [
    "How the automation finds each NS Retail control.",
    "Tracked in git: no paths, no credentials - only control identities.",
    "",
    "CONFIRMED entries were read from the real application with",
    "    scripts\\inspect_windows.bat <name> \"Victory Bazars.*\"",
    "TODO entries still need a capture of the matching screen.",
    "",
    "Progress:  .venv\\Scripts\\python.exe -m ns_retail_automation --check"
  ],

  "windows": {
    "_comment": "NS Retail is a WinForms MDI app (main form auto_id 'frmMain'). Screens open inside the main window and the title becomes 'Victory Bazars - [<screen>]'. The WindowsForms10 class-name suffix changes between launches - match on the title.",

    "main": {
      "_status": "CONFIRMED 2026-09-09 - title 'Victory Bazars', auto_id 'frmMain'",
      "title_re": "^Victory Bazars",
      "timeout_seconds": 60
    },

    "login": {
      "_status": "TODO - not needed while login.enabled is false",
      "title_re": ""
    },

    "report_viewer": {
      "_status": "CONFIRMED 2026-09-09 - MDI child 'frmReportPlaceHolder', seen as a title change",
      "title_re": "Victory Bazars - \\[Report Viewer\\]",
      "timeout_seconds": 300
    },

    "export_dialog": {
      "_status": "TODO - capture the Export To dialog",
      "title_re": ""
    },

    "save_dialog": {
      "_status": "TODO - capture the Save As dialog",
      "title_re": ""
    }
  },

  "steps": {
    "_comment": "Each step lists the controls to act on, in order.",

    "login": { "window": "login", "targets": [] },

    "open_reports": {
      "window": "main",
      "description": "CONFIRMED - the 'Reports' tab on the ribbon.",
      "targets": [
        {
          "action": "select",
          "description": "Reports ribbon tab",
          "control_type": "TabItem",
          "title": "Reports",
          "parent": { "auto_id": "ribbonControl1" }
        }
      ]
    },

    "open_stock_reports": {
      "window": "main",
      "description": "CONFIRMED - 'Stock Reports' button in the Reports ribbon group. Scoped to the ribbon because a second button with the same name lives in the left auto-hide panel.",
      "targets": [
        {
          "action": "invoke",
          "description": "Stock Reports button on the ribbon",
          "control_type": "Button",
          "title": "Stock Reports",
          "parent": { "auto_id": "ribbonControl1" }
        }
      ]
    },

    "select_purchase_report": {
      "window": "main",
      "description": "TODO - capture the screen after Stock Reports opens, where Purchases is chosen.",
      "targets": []
    },

    "set_date": {
      "window": "main",
      "description": "CONFIRMED - From/To date pickers. The Edit inside each picker has an automation id that is really a window handle and changes every launch, so it is found through its parent ComboBox instead.",
      "targets": [
        {
          "action": "set_text",
          "description": "From Date",
          "control_type": "Edit",
          "parent": { "auto_id": "dtpFromDate" },
          "value": "{date_dd_month_yyyy}"
        },
        {
          "action": "set_text",
          "description": "To Date",
          "control_type": "Edit",
          "parent": { "auto_id": "dtpToDate" },
          "value": "{date_dd_month_yyyy}"
        }
      ]
    },

    "search": {
      "window": "main",
      "description": "CONFIRMED - Search button (auto_id btnSearch).",
      "targets": [
        { "action": "invoke", "description": "Search", "auto_id": "btnSearch", "control_type": "Button" }
      ]
    },

    "generate_report": {
      "window": "main",
      "description": "CONFIRMED - Report button (auto_id btnReport).",
      "targets": [
        { "action": "invoke", "description": "Report", "auto_id": "btnReport", "control_type": "Button" }
      ]
    },

    "export_to": {
      "window": "report_viewer",
      "description": "TODO - capture the generated report and its Export To control.",
      "targets": []
    },

    "choose_csv_format": { "window": "report_viewer", "targets": [] },
    "confirm_export": { "window": "report_viewer", "targets": [] },
    "save_set_path": { "window": "save_dialog", "targets": [] },
    "save_confirm": { "window": "save_dialog", "targets": [] }
  }
}
