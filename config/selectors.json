{
  "_README": [
    "How the automation finds each NS Retail control.",
    "",
    "This file is TRACKED IN GIT so it can be developed on the Mac and pulled",
    "onto the Windows PC. It contains no paths and no credentials.",
    "",
    "Values marked CONFIRMED were read from the real application with:",
    "    scripts\\inspect_windows.bat",
    "Everything still empty is waiting for a capture from the Windows PC.",
    "",
    "Check progress at any time with:  ns-retail-automation --check"
  ],

  "windows": {
    "_comment": "NS Retail is a WinForms MDI app: screens open INSIDE the main window and the title becomes 'Victory Bazars - [<screen>]'. Match on the title; the WindowsForms10 class-name suffix is generated by .NET and is not stable.",

    "main": {
      "_status": "CONFIRMED at the office on 2026-09-08 - re-confirm on this PC",
      "title_re": "^Victory Bazars",
      "timeout_seconds": 60
    },

    "login": {
      "_status": "TODO - capture the login window",
      "title_re": ""
    },

    "report_viewer": {
      "_status": "CONFIRMED at the office on 2026-09-08 - MDI child, seen as a title change",
      "title_re": "Victory Bazars - \\[Report Viewer\\]",
      "timeout_seconds": 300
    },

    "export_dialog": {
      "_status": "TODO - only needed if Export To opens a separate window",
      "title_re": ""
    },

    "save_dialog": {
      "_status": "TODO - capture the Save As dialog (standard Windows dialogs use class #32770)",
      "title_re": ""
    }
  },

  "steps": {
    "_comment": "Each step lists the controls to act on, in order. Actions: invoke (preferred), click, select, expand, set_text, menu_select, wait.",

    "login": { "window": "login", "targets": [] },
    "open_reports": { "window": "main", "targets": [] },
    "open_stock_reports": { "window": "main", "targets": [] },
    "select_purchase_report": { "window": "main", "targets": [] },
    "set_date": { "window": "main", "targets": [] },
    "search": { "window": "main", "targets": [] },
    "generate_report": { "window": "main", "targets": [] },
    "export_to": { "window": "report_viewer", "targets": [] },
    "choose_csv_format": { "window": "report_viewer", "targets": [] },
    "confirm_export": { "window": "report_viewer", "targets": [] },
    "save_set_path": { "window": "save_dialog", "targets": [] },
    "save_confirm": { "window": "save_dialog", "targets": [] }
  }
}
